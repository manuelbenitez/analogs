name = "analogs-images"
main = "worker/index.ts"
compatibility_date = "2024-01-01"

# R2 bucket binding
[[r2_buckets]]
binding = "IMAGES"
bucket_name = "analogs-images"
preview_bucket_name = "analogs-images"

[vars]
ADMIN_PASSWORD = "change-me-in-cloudflare-dashboard"

[env.production]
vars = { ENVIRONMENT = "production" }
